{
  "name": "hydrocert-api",
  "version": "1.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "deploy:contract": "node --env-file-if-exists=.env scripts/deploy.js"
  },
  "dependencies": {
    "ethers": "^6.17.0",
    "express": "^4.19.2",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "hardhat": "^3.9.1",
    "solc": "^0.8.35"
  }
}
